import 'package:flutter/material.dart';

class BinaryPage extends StatelessWidget {
  static const tag = "Binary_page";
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(),);
  }
}
