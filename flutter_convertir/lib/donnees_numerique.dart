import 'package:flutter/material.dart';

class DonneePage extends StatelessWidget {
  static const tag = "Donnee_page";
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(),);
  }
}
