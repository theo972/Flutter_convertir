import 'package:flutter/material.dart';

class TempPage extends StatelessWidget {
  static const tag = "Temp_page";
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(),);
  }
}
