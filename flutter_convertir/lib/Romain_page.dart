import 'package:flutter/material.dart';

class RomePage extends StatelessWidget {
  static const tag = "Rome_page";
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(),);
  }
}
